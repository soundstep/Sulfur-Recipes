---
phase: 02-docker-dx
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.yml
  - .env.example
  - replit.md
autonomous: true
requirements:
  - DOCK-03
  - DOCK-04
  - DX-01
  - DX-02

must_haves:
  truths:
    - "docker compose up starts both services with no manual steps beyond cp .env.example .env"
    - "frontend loads in the browser at http://localhost:8080"
    - "recipe searches return data (frontend talks to api via /api proxy)"
    - ".env.example documents every env var required to run locally"
    - "replit.md or README.md explains how to run the app with Docker"
  artifacts:
    - path: "docker-compose.yml"
      provides: "Orchestrates api-server and frontend services"
      contains: "services:"
    - path: ".env.example"
      provides: "Template for local env vars"
      contains: "PORT="
    - path: "replit.md"
      provides: "Developer onboarding docs with local-dev section"
      contains: "docker compose"
  key_links:
    - from: "docker-compose.yml"
      to: "artifacts/api-server/Dockerfile"
      via: "build context: . (repo root)"
      pattern: "context: \\."
    - from: "docker-compose.yml"
      to: "artifacts/sulfur-recipe-finder/Dockerfile"
      via: "build context: . (repo root)"
      pattern: "context: \\."
    - from: "frontend nginx"
      to: "api-server:3000"
      via: "/api proxy (nginx.conf or compose network)"
      pattern: "proxy_pass.*api"
---

<objective>
Wire both Docker services together with docker-compose.yml, document env vars in .env.example, and add a local-dev section to replit.md.

Purpose: The Dockerfiles from plan 01 are build recipes; this plan is the runtime orchestration. `docker compose up` must be the single command that starts the full app.
Output: `docker-compose.yml` (both services, shared network, /api proxy), `.env.example` (all required vars), updated `replit.md` (local dev section).
</objective>

<execution_context>
@/Users/RomuQuan/Documents/devserver/workspace/Sulfur-Recipes/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/RomuQuan/Documents/devserver/workspace/Sulfur-Recipes/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

<interfaces>
<!-- Key facts extracted from the codebase for the executor. -->

From artifacts/api-server/src/index.ts:
- `process.env["PORT"]` is REQUIRED — throws if absent
- Recommended port: 3000 (conventional Express)

From artifacts/sulfur-recipe-finder/vite.config.ts:
- Vite preview port: `Number(process.env.PORT) || 5173`
- But the Dockerfile uses nginx:alpine (port 80) — the frontend container listens on port 80
- The frontend needs to talk to the API — check src/ for API base URL pattern

From artifacts/api-server/src/app.ts:
- Express app mounts router at `/api`
- CORS middleware is enabled (cors() with default allow-all)

Services needed:
- `api` — built from repo root, Dockerfile at artifacts/api-server/Dockerfile, exposes PORT (3000)
- `frontend` — built from repo root, Dockerfile at artifacts/sulfur-recipe-finder/Dockerfile, exposes 80

The frontend must proxy `/api` requests to the api service. Two options:
1. nginx.conf in the frontend image: `proxy_pass http://api:3000;` (preferred — no nginx.conf file needed in docker-compose, self-contained)
2. Compose-level proxy via Traefik or similar (overkill for local dev)

Use option 1: add a custom `nginx.conf` that serves static files and proxies `/api` to `http://api:3000`. The nginx.conf is baked into the frontend Dockerfile (or mounted via compose volume — volume is simpler, use it).

Env vars required:
- `PORT` — api-server port (value: 3000)
- No other env vars found in api-server (CORS is permissive, no DB)
- Frontend: `PORT` and `BASE_PATH` are optional (vite.config.ts has defaults) — not needed for nginx serve

replit.md currently describes the Replit/Vite dev workflow — add a "## Local Development (Docker)" section before the existing content.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: docker-compose.yml — wire api + frontend services</name>
  <files>docker-compose.yml, nginx.conf</files>
  <read_first>
    - artifacts/api-server/Dockerfile (port exposed, CMD)
    - artifacts/sulfur-recipe-finder/Dockerfile (port exposed — nginx 80)
    - artifacts/sulfur-recipe-finder/src/ (check how API URL is constructed — look for VITE_API_URL or fetch("/api/...") patterns)
    - artifacts/api-server/src/app.ts (confirm /api mount point)
  </read_first>
  <action>
**Step 1: Check how the frontend calls the API.**
Read `artifacts/sulfur-recipe-finder/src/` to find fetch calls. Expected pattern: `fetch("/api/recipes/...")` — relative URL, meaning the nginx proxy must handle `/api` → backend.

**Step 2: Create `nginx.conf` at repo root (or in artifacts/sulfur-recipe-finder/).**
This file will be volume-mounted into the frontend container so `/api` is proxied to the api service:

```nginx
server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # Proxy API calls to the backend service
    location /api/ {
        proxy_pass http://api:3000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    # SPA fallback — serve index.html for any non-file route
    location / {
        try_files $uri $uri/ /index.html;
    }
}
```

Place this file at `nginx.conf` in the repo root.

**Step 3: Create `docker-compose.yml` at repo root.**

```yaml
version: "3.9"

services:
  api:
    build:
      context: .
      dockerfile: artifacts/api-server/Dockerfile
    env_file: .env
    ports:
      - "3000:3000"
    networks:
      - app

  frontend:
    build:
      context: .
      dockerfile: artifacts/sulfur-recipe-finder/Dockerfile
    ports:
      - "8080:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - api
    networks:
      - app

networks:
  app:
    driver: bridge
```

Key points:
- Both services use `context: .` (repo root) — required for workspace packages
- `api` uses `env_file: .env` to get PORT=3000
- `frontend` mounts `nginx.conf` to override the default nginx site config
- `frontend` depends_on api (start order only — nginx will retry on upstream failure)
- Both on `app` network so `http://api:3000` resolves inside nginx
- Frontend maps host port 8080 → container port 80 (avoids needing sudo for port 80 on Linux)
  </action>
  <verify>
    <automated>docker compose config --quiet && echo "docker-compose.yml is valid"</automated>
  </verify>
  <done>
    - `docker-compose.yml` exists at repo root
    - `nginx.conf` exists at repo root
    - `docker compose config` exits 0 (valid YAML, valid compose syntax)
    - docker-compose.yml contains `services:`, `api:`, `frontend:`
    - docker-compose.yml contains `context: .` for both services
    - nginx.conf contains `proxy_pass http://api:3000`
    - nginx.conf contains `try_files $uri $uri/ /index.html` (SPA fallback)
  </done>
</task>

<task type="auto">
  <name>Task 2: .env.example + local-dev docs in replit.md</name>
  <files>.env.example, replit.md</files>
  <read_first>
    - artifacts/api-server/src/index.ts (PORT is required — confirm no other env vars)
    - artifacts/api-server/src/app.ts (any other process.env references?)
    - artifacts/api-server/src/routes/ (any env vars in route handlers?)
    - replit.md (current content — prepend local-dev section, don't delete existing content)
  </read_first>
  <action>
**Step 1: Scan for all env vars used in api-server.**
Search `artifacts/api-server/src/` for `process.env` references. Expected: only PORT. If others found, document them all.

**Step 2: Create `.env.example` at repo root.**
```env
# Sulfur Recipes — Local Development Environment
# Copy to .env: cp .env.example .env

# API server port (required — server throws if absent)
PORT=3000
```

If Step 1 found additional env vars, add them with comments explaining what they do and where to get values.

**Step 3: Prepend local-dev section to `replit.md`.**
Read the current `replit.md` content first, then write the full updated file with the new section prepended at the top:

```markdown
## Local Development (Docker)

### Prerequisites
- [Docker Desktop](https://www.docker.com/products/docker-desktop/) (or Docker Engine + Compose plugin)

### Quick Start

```bash
# 1. Copy env template
cp .env.example .env

# 2. Build images and start services
docker compose up --build

# 3. Open in browser
#    Frontend: http://localhost:8080
#    API:      http://localhost:3000/api/health
```

### Services

| Service  | URL                          | Description            |
|----------|------------------------------|------------------------|
| Frontend | http://localhost:8080        | Vite app (nginx)       |
| API      | http://localhost:3000        | Express server         |

### Stopping

```bash
docker compose down
```

### Rebuilding after code changes

```bash
docker compose up --build
```

---

{original replit.md content below}
```

Keep all existing replit.md content intact below the new section.
  </action>
  <verify>
    <automated>test -f .env.example && grep -q "PORT=3000" .env.example && grep -q "docker compose" replit.md && echo "DX artifacts OK"</automated>
  </verify>
  <done>
    - `.env.example` exists at repo root
    - `.env.example` contains `PORT=3000` with a comment explaining it is required
    - `replit.md` contains `docker compose up` in a "Local Development" section
    - `replit.md` contains `http://localhost:8080` (frontend URL)
    - Existing `replit.md` content is preserved below the new section
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    docker-compose.yml wiring api + frontend, nginx.conf proxying /api, .env.example with PORT=3000, replit.md local-dev section added.
  </what-built>
  <how-to-verify>
    1. `cp .env.example .env`
    2. `docker compose up --build` — watch logs, both services should start
    3. Open http://localhost:8080 — Vite app should load
    4. Search for a recipe — results should return (confirms /api proxy works)
    5. Open http://localhost:3000/api/health — should return 200
    6. `docker compose down`
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .env file | Contains PORT — no secrets at this stage, but .env is gitignored by convention |
| nginx proxy | Proxies /api to internal api service — not exposed to public internet (local dev only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Information Disclosure | .env file committed to git | mitigate | Add `.env` to `.gitignore`; only `.env.example` is committed |
| T-02-05 | Elevation of Privilege | nginx proxy_pass exposes all /api routes | accept | Local dev only; CORS already handled by Express |
| T-02-06 | Spoofing | No auth on API | accept | App has no user accounts — wiki scraper, no PII |
</threat_model>

<verification>
```bash
# Validate compose file
docker compose config --quiet

# Full integration test
cp .env.example .env
docker compose up --build -d
sleep 10

# Frontend serves index.html
curl -sf http://localhost:8080/ | grep -q "<html" && echo "Frontend OK"

# API health
curl -sf http://localhost:3000/api/health && echo "API OK"

# API proxy through nginx
curl -sf http://localhost:8080/api/health && echo "Proxy OK"

docker compose down
```
</verification>

<success_criteria>
- `docker compose up --build` starts both services with no errors
- http://localhost:8080 loads the Vite React app
- http://localhost:8080/api/health returns 200 (nginx proxy works)
- `.env.example` documents PORT with explanation
- `replit.md` has Local Development section with docker compose instructions
- `.env` is in `.gitignore` (or added if not already present)
</success_criteria>

<output>
After completion, create `.planning/phases/02-docker-dx/02-02-SUMMARY.md` using the summary template.
</output>
