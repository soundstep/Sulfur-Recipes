---
phase: 02-docker-dx
plan: 02
type: execute
wave: 1
depends_on: [02-01]
files_modified:
  - docker-compose.yml
  - .env.example
  - replit.md
autonomous: true
requirements:
  - DOCK-03
  - DOCK-04
  - DX-01
  - DX-02

must_haves:
  truths:
    - "docker compose up starts the app with no manual steps beyond cp .env.example .env"
    - "http://localhost:3000 serves the frontend"
    - "http://localhost:3000/api/health returns 200"
    - ".env.example documents every required env var"
    - "replit.md explains how to run with Docker"
  artifacts:
    - path: "docker-compose.yml"
      provides: "Single-service compose file"
      contains: "services:"
    - path: ".env.example"
      provides: "Template for local env vars"
      contains: "PORT="
    - path: "replit.md"
      provides: "Local dev docs"
      contains: "docker compose"
  key_links:
    - from: "docker-compose.yml"
      to: "Dockerfile"
      via: "build context: . (repo root)"
      pattern: "context: \\."
---

<objective>
Wire the single Docker image into docker-compose.yml and document local dev.

Purpose: One command (`docker compose up --build`) starts the full app. .env.example provides the only config needed. replit.md tells developers how to run it.
Output: `docker-compose.yml` (single service), `.env.example` (PORT=3000), updated `replit.md` (local-dev section prepended).
</objective>

<execution_context>
@/Users/RomuQuan/Documents/devserver/workspace/Sulfur-Recipes/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/RomuQuan/Documents/devserver/workspace/Sulfur-Recipes/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

<interfaces>
From artifacts/api-server/src/index.ts:
- `process.env["PORT"]` is REQUIRED — throws if absent
- Recommended value: 3000

From Dockerfile (plan 02-01):
- Single image, single port: 3000
- No other env vars required

From replit.md:
- Read current content before writing — prepend new section, keep existing content
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: docker-compose.yml</name>
  <files>docker-compose.yml</files>
  <action>
Create `docker-compose.yml` at repo root:

```yaml
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    env_file: .env
    ports:
      - "3000:3000"
```

Simple as possible — one service, one port, env from .env file.
No networks block needed (single service, no inter-service communication).
  </action>
  <verify>
    <automated>docker compose config --quiet && echo "docker-compose.yml valid"</automated>
  </verify>
  <done>
    - `docker-compose.yml` exists at repo root
    - `docker compose config` exits 0
    - Contains single service `app`
    - `context: .` for build
    - `ports: ["3000:3000"]`
  </done>
</task>

<task type="auto">
  <name>Task 2: .env.example</name>
  <files>.env.example</files>
  <read_first>
    - artifacts/api-server/src/index.ts (PORT is the only required env var)
    - artifacts/api-server/src/routes/ (scan for any other process.env references)
  </read_first>
  <action>
Scan `artifacts/api-server/src/` for all `process.env` references. Expected: only PORT.

Create `.env.example` at repo root:

```env
# Sulfur Recipes — Local Development
# Copy to .env: cp .env.example .env

# API server port (required — server throws if absent)
PORT=3000
```

Also verify `.env` is in `.gitignore`. If not, add it.
  </action>
  <verify>
    <automated>test -f .env.example && grep -q "PORT=3000" .env.example && echo ".env.example OK"</automated>
  </verify>
  <done>
    - `.env.example` exists with `PORT=3000`
    - `.env` is in `.gitignore`
  </done>
</task>

<task type="auto">
  <name>Task 3: Local dev section in replit.md</name>
  <files>replit.md</files>
  <read_first>
    - replit.md (full current content — must be preserved below new section)
  </read_first>
  <action>
Prepend a Local Development section to `replit.md`. Read the file first, then write the full updated content:

```markdown
## Local Development (Docker)

### Prerequisites
- [Docker Desktop](https://www.docker.com/products/docker-desktop/) (or Docker Engine + Compose plugin)

### Quick Start

\`\`\`bash
# 1. Copy env template
cp .env.example .env

# 2. Build image and start
docker compose up --build

# 3. Open in browser
#    App: http://localhost:3000
\`\`\`

### Endpoints

| Path | Description |
|------|-------------|
| `http://localhost:3000` | Frontend (Vite build) |
| `http://localhost:3000/api/health` | API health check |
| `http://localhost:3000/api/recipes` | Recipes endpoint |

### Stopping

\`\`\`bash
docker compose down
\`\`\`

### Rebuilding after code changes

\`\`\`bash
docker compose up --build
\`\`\`

---

{original replit.md content}
```

Keep all existing `replit.md` content intact below the separator.
  </action>
  <verify>
    <automated>grep -q "docker compose" replit.md && grep -q "localhost:3000" replit.md && echo "replit.md OK"</automated>
  </verify>
  <done>
    - `replit.md` has Local Development section at top
    - Contains `docker compose up --build`
    - Contains `http://localhost:3000`
    - Existing content preserved below
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Single Dockerfile (Express serves Vite build + API), docker-compose.yml (one service, port 3000), .env.example, replit.md local-dev docs.
  </what-built>
  <how-to-verify>
    1. `cp .env.example .env`
    2. `docker compose up --build` — watch logs, server should start on port 3000
    3. Open http://localhost:3000 — app should load
    4. Search for a recipe — results should return (confirms API works)
    5. Open http://localhost:3000/api/health — should return 200
    6. `docker compose down`
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
```bash
cp .env.example .env
docker compose up --build -d
sleep 10

curl -sf http://localhost:3000/ | grep -q "<html" && echo "Frontend OK"
curl -sf http://localhost:3000/api/health && echo "API OK"

docker compose down
```
</verification>

<success_criteria>
- `docker compose up --build` starts the app with no errors
- http://localhost:3000 serves the React app
- http://localhost:3000/api/health returns 200
- `.env.example` has PORT=3000 with comment
- `replit.md` has local-dev section
- `.env` is gitignored
</success_criteria>

<output>
After completion, create `.planning/phases/02-docker-dx/02-02-SUMMARY.md` using the summary template.
</output>
