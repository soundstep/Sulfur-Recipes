---
phase: 01-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pnpm-workspace.yaml
  - tsconfig.json
  - lib/db/package.json
autonomous: true
requirements:
  - CLEAN-01
  - CLEAN-04

must_haves:
  truths:
    - "lib/db package no longer exists in the workspace"
    - "pnpm-workspace.yaml has no reference to lib/db or postgres/drizzle deps in catalog"
    - "root tsconfig.json has no reference to ./lib/db"
    - "pnpm install completes without errors after removal"
  artifacts:
    - path: "pnpm-workspace.yaml"
      provides: "Workspace package list and catalog"
      contains: "packages:"
    - path: "tsconfig.json"
      provides: "Root TypeScript project references"
  key_links:
    - from: "pnpm-workspace.yaml"
      to: "lib/db"
      via: "packages glob"
      pattern: "lib/\\*"
    - from: "tsconfig.json"
      to: "lib/db"
      via: "references"
      pattern: "path.*lib/db"
---

<objective>
Remove the `lib/db` workspace package and all PostgreSQL/Drizzle dependencies from the monorepo root configuration.

Purpose: The app has no persistence needs — all data comes from the wiki scraper cached in memory. The DB package and its dependencies are dead weight that blocks running the project locally.
Output: Deleted `lib/db/` directory, cleaned `pnpm-workspace.yaml` catalog and packages list, cleaned root `tsconfig.json` references.
</objective>

<execution_context>
@/Users/RomuQuan/Documents/devserver/workspace/Sulfur-Recipes/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/RomuQuan/Documents/devserver/workspace/Sulfur-Recipes/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete lib/db package directory</name>
  <files>lib/db/ (entire directory — delete)</files>
  <read_first>
    - lib/db/package.json — confirm what is being deleted
    - lib/db/src/ — confirm no files are depended on by other packages
  </read_first>
  <action>
    Delete the entire `lib/db/` directory recursively:
    ```
    rm -rf lib/db
    ```
    Confirm no other package in the workspace imports from `@workspace/db`. Grep for the import pattern:
    ```
    grep -r "@workspace/db" artifacts/ lib/ scripts/ --include="*.ts" --include="*.tsx" --include="*.js"
    ```
    If any imports are found, remove those import statements or the files that use them (there should be none — the app is memory-only).
  </action>
  <verify>
    <automated>test ! -d lib/db && echo "PASS: lib/db deleted" || echo "FAIL: lib/db still exists"</automated>
  </verify>
  <done>The `lib/db/` directory is gone from the filesystem. No other workspace file imports `@workspace/db`.</done>
</task>

<task type="auto">
  <name>Task 2: Clean pnpm-workspace.yaml and root tsconfig.json</name>
  <files>pnpm-workspace.yaml, tsconfig.json</files>
  <read_first>
    - pnpm-workspace.yaml — read current content before editing
    - tsconfig.json — read current content before editing
  </read_first>
  <action>
    **pnpm-workspace.yaml** — make these changes:

    1. Remove `- lib/*` from the `packages:` list (this removes all lib packages from the workspace glob). The remaining packages should be:
       ```yaml
       packages:
         - artifacts/*
         - lib/integrations/*
         - scripts
       ```
       (Remove `- lib/*` line only. Keep `- lib/integrations/*` if it exists and has real packages, otherwise remove it too — check `lib/integrations/` contents first.)

    2. Remove the following entries from the `catalog:` block entirely:
       - `drizzle-orm: ^0.45.1`
       - `drizzle-zod: ...` (if present — it's in lib/db's package.json as a direct dep, may not be in catalog)

    3. Remove the following entries from the `catalog:` block (Replit plugins — will be unused after Plan 02 cleans vite.config.ts):
       - `'@replit/vite-plugin-cartographer': ^0.5.1`
       - `'@replit/vite-plugin-dev-banner': ^0.1.1`
       - `'@replit/vite-plugin-runtime-error-modal': ^0.0.6`

    Note: `pg`, `@types/pg`, `drizzle-kit` appear in `lib/db/package.json` but NOT in the root catalog. No catalog removal needed for those — they'll vanish when `lib/db` is deleted and `pnpm install` is re-run.

    **tsconfig.json** — remove the `./lib/db` reference entry:
    ```json
    {
      "extends": "./tsconfig.base.json",
      "compileOnSave": false,
      "files": [],
      "references": [
        { "path": "./lib/api-client-react" },
        { "path": "./lib/api-zod" }
      ]
    }
    ```
    (Remove only the `{ "path": "./lib/db" }` entry. Keep the other references intact.)

    After editing both files, run:
    ```
    pnpm install
    ```
    to update the lockfile and confirm no resolution errors.
  </action>
  <verify>
    <automated>
      grep -v "^#" pnpm-workspace.yaml | grep -c "lib/db" || echo "0 matches (PASS)"
      grep -c '"path": "./lib/db"' tsconfig.json || echo "0 matches (PASS)"
      grep -c "drizzle-orm" pnpm-workspace.yaml || echo "0 drizzle-orm in catalog (PASS)"
      grep -c "@replit/vite-plugin" pnpm-workspace.yaml || echo "0 replit plugins in catalog (PASS)"
    </automated>
  </verify>
  <done>
    - `pnpm-workspace.yaml` packages list has no `lib/*` glob
    - `pnpm-workspace.yaml` catalog has no `drizzle-orm`, no `@replit/vite-plugin-*` entries
    - `tsconfig.json` references array has no `./lib/db` entry
    - `pnpm install` exits 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local filesystem → pnpm | File edits affect dependency resolution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | pnpm-workspace.yaml | accept | Manual cleanup task — executor reads file before editing; low risk, no secrets |
</threat_model>

<verification>
```bash
# Confirm lib/db is gone
test ! -d lib/db

# Confirm no DB imports survive
grep -r "@workspace/db" artifacts/ lib/ scripts/ --include="*.ts" --include="*.tsx" 2>/dev/null | wc -l
# expected: 0

# Confirm workspace config clean
grep -c "lib/db" pnpm-workspace.yaml tsconfig.json
# expected: 0 0

# Confirm install works
pnpm install --frozen-lockfile 2>&1 | tail -5
```
</verification>

<success_criteria>
- `lib/db/` directory does not exist
- `pnpm-workspace.yaml` has no `lib/db` reference and no Drizzle/Replit catalog entries
- `tsconfig.json` has no `./lib/db` reference
- `pnpm install` exits without errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-cleanup/01-01-SUMMARY.md`
</output>
