---
phase: 01-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - artifacts/sulfur-recipe-finder/vite.config.ts
  - artifacts/sulfur-recipe-finder/package.json
autonomous: true
requirements:
  - CLEAN-02
  - CLEAN-03

must_haves:
  truths:
    - "vite.config.ts builds without PORT or BASE_PATH env vars set"
    - "No Replit Vite plugins are imported or used in vite.config.ts"
    - "Dev server defaults to port 5173 and base /"
  artifacts:
    - path: "artifacts/sulfur-recipe-finder/vite.config.ts"
      provides: "Vite build config"
      contains: "no @replit/ imports, no throw on missing PORT/BASE_PATH"
    - path: "artifacts/sulfur-recipe-finder/package.json"
      provides: "Frontend package deps"
      contains: "no @replit/vite-plugin-* entries"
  key_links:
    - from: "artifacts/sulfur-recipe-finder/vite.config.ts"
      to: "vite dev server"
      via: "server.port"
      pattern: "port: 5173"
---

<objective>
Remove all Replit Vite plugins from `artifacts/sulfur-recipe-finder` and rewrite `vite.config.ts` so it never throws on missing `PORT` or `BASE_PATH` env vars — using sensible local defaults instead.

Purpose: The current config throws at startup without Replit-injected env vars, making local dev impossible.
Output: Clean `vite.config.ts` with no Replit plugin imports, defaults to port 5173 and base `/`.
</objective>

<execution_context>
@/Users/RomuQuan/Documents/devserver/workspace/Sulfur-Recipes/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/RomuQuan/Documents/devserver/workspace/Sulfur-Recipes/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
</context>

<interfaces>
<!-- Current vite.config.ts key facts for executor -->
<!-- File: artifacts/sulfur-recipe-finder/vite.config.ts -->

Current Replit imports to remove:
```typescript
import runtimeErrorOverlay from "@replit/vite-plugin-runtime-error-modal";
// dynamic: @replit/vite-plugin-cartographer
// dynamic: @replit/vite-plugin-dev-banner
```

Current throw pattern to replace:
```typescript
const rawPort = process.env.PORT;
if (!rawPort) { throw new Error("PORT environment variable is required..."); }
const port = Number(rawPort);
// ... similar block for BASE_PATH
```

Target pattern (after rewrite):
```typescript
const port = Number(process.env.PORT) || 5173;
const basePath = process.env.BASE_PATH || "/";
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite vite.config.ts — remove Replit plugins, use safe defaults</name>
  <files>artifacts/sulfur-recipe-finder/vite.config.ts</files>
  <read_first>
    - artifacts/sulfur-recipe-finder/vite.config.ts — full current file before touching anything
  </read_first>
  <action>
Rewrite `artifacts/sulfur-recipe-finder/vite.config.ts` to the following exact content (preserving non-Replit config):

```typescript
import { defineConfig } from "vite";
import react from "@vitejs/plugin-react";
import tailwindcss from "@tailwindcss/vite";
import path from "path";

const port = Number(process.env.PORT) || 5173;
const basePath = process.env.BASE_PATH || "/";

export default defineConfig({
  base: basePath,
  plugins: [
    react(),
    tailwindcss(),
  ],
  resolve: {
    alias: {
      "@": path.resolve(import.meta.dirname, "src"),
      "@assets": path.resolve(import.meta.dirname, "..", "..", "attached_assets"),
    },
    dedupe: ["react", "react-dom"],
  },
  root: path.resolve(import.meta.dirname),
  build: {
    outDir: path.resolve(import.meta.dirname, "dist/public"),
    emptyOutDir: true,
  },
  server: {
    port,
    host: "0.0.0.0",
    allowedHosts: true,
    fs: {
      strict: true,
      deny: ["**/.*"],
    },
  },
  preview: {
    port,
    host: "0.0.0.0",
    allowedHosts: true,
  },
});
```

Key changes vs original:
- Removed: `import runtimeErrorOverlay from "@replit/vite-plugin-runtime-error-modal"`
- Removed: throw blocks for PORT and BASE_PATH
- Removed: dynamic imports of `@replit/vite-plugin-cartographer` and `@replit/vite-plugin-dev-banner`
- Removed: `runtimeErrorOverlay()` from plugins array
- Changed: `const port = Number(process.env.PORT) || 5173` (safe default)
- Changed: `const basePath = process.env.BASE_PATH || "/"` (safe default)
  </action>
  <verify>
    <automated>grep -c "@replit" artifacts/sulfur-recipe-finder/vite.config.ts | grep -q "^0$" && echo "@replit imports removed ✓" || echo "FAIL: @replit still present" && grep "throw" artifacts/sulfur-recipe-finder/vite.config.ts && echo "FAIL: throw still present" || echo "no throws ✓" && grep "5173" artifacts/sulfur-recipe-finder/vite.config.ts && echo "default port present ✓" || echo "FAIL: default port missing"</automated>
  </verify>
  <done>
    - `grep "@replit" artifacts/sulfur-recipe-finder/vite.config.ts` returns empty
    - `grep "throw" artifacts/sulfur-recipe-finder/vite.config.ts` returns empty
    - File contains `|| 5173` and `|| "/"`
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove Replit Vite plugin deps from package.json</name>
  <files>artifacts/sulfur-recipe-finder/package.json</files>
  <read_first>
    - artifacts/sulfur-recipe-finder/package.json — current devDependencies
  </read_first>
  <action>
Edit `artifacts/sulfur-recipe-finder/package.json`: remove these entries from `devDependencies` (or `dependencies`):
- `@replit/vite-plugin-runtime-error-modal`
- `@replit/vite-plugin-cartographer`
- `@replit/vite-plugin-dev-banner`

Also remove the same entries from the root `pnpm-workspace.yaml` catalog section if they appear there (they do — lines starting with `'@replit/vite-plugin-*'`).

After editing, run `pnpm install` from repo root to update the lockfile.
  </action>
  <verify>
    <automated>grep "@replit/vite-plugin" artifacts/sulfur-recipe-finder/package.json && echo "FAIL: Replit deps still in package.json" || echo "package.json clean ✓"</automated>
  </verify>
  <done>
    - `grep "@replit/vite-plugin" artifacts/sulfur-recipe-finder/package.json` returns empty
    - `pnpm install` completes without unresolvable dependency errors
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env vars → vite config | PORT/BASE_PATH now optional; default values are safe for local dev |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02 | Information Disclosure | vite dev server on 0.0.0.0 | accept | Local dev only; same as original config; not a production server |
</threat_model>

<verification>
```bash
# No Replit imports
grep "@replit" artifacts/sulfur-recipe-finder/vite.config.ts && echo "FAIL" || echo "PASS"

# No throws in config
grep "throw" artifacts/sulfur-recipe-finder/vite.config.ts && echo "FAIL" || echo "PASS"

# Safe defaults present
grep "5173\||| \"/\"" artifacts/sulfur-recipe-finder/vite.config.ts

# No Replit plugin deps
grep "@replit/vite-plugin" artifacts/sulfur-recipe-finder/package.json && echo "FAIL" || echo "PASS"

# Vite config parses (no syntax errors)
cd artifacts/sulfur-recipe-finder && node --input-type=module <<< "import './vite.config.ts'" 2>&1 | head -5
```
</verification>

<success_criteria>
- `vite.config.ts` has zero `@replit` imports
- `vite.config.ts` has zero `throw` statements
- Dev server defaults to port 5173, base `/` when env vars absent
- `package.json` for sulfur-recipe-finder has no `@replit/vite-plugin-*` dependencies
- `pnpm install` succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/01-cleanup/01-02-SUMMARY.md`
</output>
